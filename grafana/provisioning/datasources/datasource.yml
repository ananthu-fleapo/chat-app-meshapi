apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Local dev: set PROMETHEUS_URL=http://prometheus:9090
    # Cloud Run: set PROMETHEUS_URL=https://<prometheus-cloud-run-url>
    url: ${PROMETHEUS_URL}
    isDefault: true
    editable: false