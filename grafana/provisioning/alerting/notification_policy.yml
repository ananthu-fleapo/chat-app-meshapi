apiVersion: 1

policies:
  - receiver: Slack
    group_by:
      - alertname
      - severity
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: Slack
        matchers:
          - severity = critical
        repeat_interval: 1h
